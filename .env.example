# Optional: full path to terminal64.exe (required on most VPS if MT5 is not in default folder)
# MT5_TERMINAL_PATH=C:\Program Files\MetaTrader 5\terminal64.exe

# Cloud / VPS — auto-restart after crash
RESTART_DELAY_SECONDS=30
HEARTBEAT_SECONDS=60
